import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'crazy-yoda-chat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
